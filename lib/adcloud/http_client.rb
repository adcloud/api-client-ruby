module Adcloud
  
  class HTTPClient

    def post(path, data)
      # token = Adcloud::Authentication.token
      # Farraday.post(url, token, data)
    end

    def get(path, data)
      # token = Adcloud::Authentication.token      
      # Farraday.get(url, token, data)      
    end

  end
end
