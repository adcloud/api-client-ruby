module Adcloud
  
  class HTTPClient

    def post(path, data)
      # token = Adcloud::Oauth.token
      # Farraday.post(url, token, data)
    end

    def get(path, data)
      # token = Adcloud::Oauth.token      
      # Farraday.get(url, token, data)      
    end

  end
end
